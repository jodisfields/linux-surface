##
## Fix CONFIG_SCSI_UFSHCD mismatch between kernel-ark source tree (=y)
## and what Kconfig actually resolves to (=m) for kernel-6.19.8-0.
## Also set CONFIG_SCSI_UFSHCD_PCI explicitly to avoid "unset config
## items" errors on arm64.
##
CONFIG_SCSI_UFSHCD=m
# CONFIG_SCSI_UFSHCD_PCI is not set
